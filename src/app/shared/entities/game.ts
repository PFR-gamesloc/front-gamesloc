export interface Game {
    gameId: number,
    gameName: string,
    gameDescr: string,
    stock: number,
    gamePrice: number,
    image: string,
    minPlayer: number,
    maxPlayer: number,
    minAge: number 
}
