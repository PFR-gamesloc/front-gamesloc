export interface Game {
    gameId: number,
    gameName: string,
    gameDescription: string,
    stock: number,
    gamePrice: number,
    image: string,
    minPlayer: number,
    maxPlayer: number,
    minAge: number
}
